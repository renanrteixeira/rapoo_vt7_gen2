---
topic: Rapoo VT7 Linux app: systray battery + full mouse feature set (DPI, performance, remap, system)
updated: 2026-08-10T20:11
---

- (event) bmad-spec run 1: distill CONTEXT.md + docs/FEATURES.md into SPEC-rapoo-vt7; slug=rapoo-vt7, scope=product complete (battery implemented + Phase 2+ roadmap pending)
- (direction) Scope is the complete product, not only the next slice; implemented capabilities are marked as such in the spec.
- (capability) CAP-1 Battery indicator (implemented): systray icon with percentage inside, color green>=50 / yellow 20-49 / red<20, charging bolt when on cable, low-battery notification at 20% and 10%; success = live validated value (62%) shows in correct color band and triggers notifications at thresholds.
- (capability) CAP-2 Both connections with hot swap (implemented): app works on 2.4G receiver (PID 0x1413, prefix 0xA5) and USB cable (PID 0x4613, prefix 0xFF), prefers cable, switches interface on timeout; success = plug/unplug cable without restart and connection mode stays truthful.
- (capability) CAP-3 Robust link / sleep handling (implemented): auto-reconnect ~5 s, sleep detected by empty reply (06 00...), listen-only quiet mode while asleep (no command spam), wake on first passive report 7; success = user movement wakes the app and disconnects recover by themselves.
- (capability) CAP-4 EEPROM infrastructure (pending, Phase 0): read and write any bank-0 EEPROM field (2B LE address, len<=24), JSON baseline dump before any write, immediate re-read to verify each write; success = write_eeprom re-reads new value and probe.py --dump produces baseline JSON.
- (capability) CAP-5 DPI control (pending, Phase 2): show current gear + DPI X/Y, switch gear, set value 50-26000 step 50, enable/disable gears, all from the systray menu; success = gear switch persists across reboot and a set value re-reads identically.
- (capability) CAP-6 Performance / sensor parameters (pending, Phase 3): adjust performance mode 0x08DC, RF strategy 0x08D8, polling rate, and the mouse-parameter set (motion sync, corrections, sensor angle, glass, debounces, lift-off, DC switch, sleep, low power) from the menu; success = each parameter flows read, show, write, re-read, persist and is confirmed on device.
- (capability) CAP-7 Button remap (pending, Phase 4): map each button field 0x0600-0x0638 to a function, always keeping at least one left button functional; success = remap written, re-read to chosen code, and OS receives the new function on click.
- (capability) CAP-8 System operations (pending, Phase 5): factory reset 0xAD with confirmation dialog, device name 0x09EC, receiver pairing 3-step flow; success = each performed and verified on the device without corrupting state.
- (constraint) Direct hidraw only: python3-hid/hidapi cannot open the device; use os.open(/dev/hidrawX, O_RDWR|O_NONBLOCK) + select, no feature-report ioctl (the reply arrives on input report 6).
- (constraint) VT_nrf54L protocol only: Output Report ID 6, 32 B, payload [prefix, cmdId, ...], prefix 0xA5 (receiver) / 0xFF (USB), no +32 on cmdId; ClickSync A5A5/A5A4 is another generation and is forbidden.
- (constraint) EEPROM golden rule: dump a JSON baseline before any write, verify every write by immediate re-read, read in blocks of at most 24 B (firmware limit).
- (constraint) Mouse-asleep discipline: commands may answer empty (06 00...); minimize command load — 0xAA only on first connect and after 300 s without report 7, listen-only (60 s quiet) otherwise.
- (constraint) Identification: the configuration interface is the hidraw whose report descriptor contains Report ID 6; udev must cover both PIDs 24ae:1413 and 24ae:4613 with MODE=0664 GROUP=plugdev.
- (constraint) Stack: Python 3, GTK3 + AppIndicator (ubuntu-appindicators extension, status ACTIVE), pycairo icons (not gi.repository.cairo), absolute PNG path passed to set_icon_full, panel icon-size 24 px for readability.
- (constraint) Linux-only, single user: Ubuntu 26.04 + GNOME Wayland; lifecycle via install.sh / run.sh / uninstall.sh and an autostart desktop file.
- (non-goal) No RGB/lighting or OLED/screen features: product config 17939 has no lightModes/hasScreen; interface 2 (hidraw3, 512 B) is unused by the VT7.
- (non-goal) Firmware update (0xA8) deferred: requires wired mode + factory dump and is high risk; only as a last phase.
- (non-goal) Macros out of scope until VT7 macro support is confirmed on device (the bundle macro protocol appears to be from another generation).
- (non-goal) No Windows/macOS, no other Rapoo devices, no full settings window — menu + dialogs only.
- (assumption) EEPROM addresses and names in FEATURES.md section 2 come from the A Hub bundle and map to the real VT7; the 1B vs 2B LE formats marked low-confidence are assumed pending Phase 1 validation.
- (assumption) The VT7 has no RGB/OLED (product config 17939 lacks lightModes and hasScreen).
- (assumption) The A Hub bundle (docs/rapoo_hub_app.js) is the authoritative protocol reference.
- (assumption) Single-user personal tool (user renan, intermediate skill); no auth or multiuser concerns.
- (question) Are the low-confidence field formats (1B vs 2B LE) correct for each FEATURES.md section 2 field?
- (question) Does switching the DPI gear persist across reboot on this firmware?
- (question) Which EEPROM field maps lift-off height (candidates include MOUSE_SLIGHT 0x0884)?
- (question) Are the button-remap function numeric codes extractable from the minified A Hub bundle?
- (question) What is the index-to-Hz map for polling rate (rpt_24g and rpt_usb in report 7)?
- (question) Is 0x08D8 a shared bit mask between RF strategy and the low-power warning?
- (question) Does the passive report 7 mirror every field Phase 3 needs for cross-validation?
- (note) Test suite exists (tests/test_battery.py via python3 -m unittest) and README documents install/run/probe/uninstall commands.
- (event) Self-validate pass 1 (coherence): all 8 capabilities carry intent+success, constraints bend design, non-goals explicit, success signal testable, IDs unique, prose lean — PASS.
- (event) Self-validate pass 2 (preservation): walked CONTEXT.md + docs/FEATURES.md claim by claim; all load-bearing claims landed in SPEC.md, phase-map.md, or the adopted companions (CONTEXT.md, FEATURES.md) — PASS. Wrapper-only content (raw byte offsets, hex examples, troubleshooting table, command listings) intentionally retained in the companions.
- (event) SPEC-rapoo-vt7 finalized (8 capabilities, 1 spec-authored companion + 2 adopted).
- (decision) Story S1 (CAP-4, Phase 0): EEPROM write + verify in device.py (0xA5, 2B LE addr, len<=24, immediate re-read); unit tests; spec_checkpoint=true.
- (decision) Story S2 (CAP-4, Phase 0): probe.py --dump baseline JSON (24 B blocks, ~/.cache/rapoo-vt7/eeprom_baseline.json) + settings.py named fields with range validation; done_checkpoint=true.
- (decision) Story S3 (CAP-4, Phase 1): probe.py --status full read of section 2, resolve 1B/2B LE formats, cross-validate with report 7, record validated results in docs/FEATURES.md; answers OQ 1/3/6/7; done_checkpoint=true.
- (decision) Story S4 (CAP-5, Phase 2): DPI in systray menu (gear + X/Y read, switch gear 0x0898, set value 0x0888/0x08C8 50-26000 step 50, enable/disable 0x0896, reboot persistence).
- (decision) Story S5 (CAP-6, Phase 3): performance mode 0x08DC 6-mode submenu with real sensor-change validation.
- (decision) Story S6 (CAP-6, Phase 3): RF strategy 0x08D8 shared bit mask + polling rate index-to-Hz map; answers OQ 5/6.
- (decision) Story S7 (CAP-6, Phase 3): mouse parameter toggles (motion sync, corrections, sensor angle, glass, debounce, lift-off, DC, sleep, low power) with read-show-write-re-read flow.
- (decision) Story S8 (CAP-7, Phase 4): button remap — extract function codes from bundle (A Hub diff), key-to-function UI, write 0x0600-0x0638, enforce >=1 left button; answers OQ 4; spec_checkpoint=true.
- (decision) Story S9 (CAP-8, Phase 5): system ops — factory reset 0xAD with confirmation, device name 0x09EC, receiver pairing 3-step; spec_checkpoint=true.
- (event) Story Breakdown complete: 9 stories, checkpoints set by human — spec_checkpoint on S1/S8/S9, done_checkpoint on S2/S3.
- (event) stories.yaml schema check: 9 entries, all required fields present, ids unique and prefix-free, no status field, ids quoted strings — PASS.
- (direction) Resolution (interface decision, raised by user before architecture): GUI is the sole user surface — every user-manipulable feature (CAP-5 to CAP-8) must be reachable from the systray menu or a dialog; the CLI (probe.py) is a diagnostics/validation harness only, never a user-facing interface. Display (report 7) vs manipulation (menu) stays distinct.
- (constraint) GUI-only user surface: every user-manipulable feature (CAP-5-8) is reachable from the systray menu or a dialog; the CLI (probe.py) is a diagnostics/validation harness, never a user-facing interface.
- (event) SPEC.md re-derived with the GUI-surface constraint; self-validate pass 1 re-run: constraint bends design (forces a menu/dialog piece into every CAP-5-8 story, demotes CLI), capabilities and IDs unchanged — PASS.
- (decision) Adopt ARCHITECTURE-SPINE.md (architecture-rapoo_vt7_gen2-2026-08-10, AD-1..AD-10) as an adopted companion of SPEC-rapoo-vt7; AD IDs are stable and govern implementation; referenced by relative path, owned by bmad-architecture, not edited here.
- (event) SPEC.md frontmatter companions updated with the spine path.
- (event) Self-validate pass 1+2 re-run after companion adoption: capabilities/constraints/IDs unchanged; spine referenced, not duplicated; preservation intact — PASS.
- (event) S3 done on real device (probe.py --status, 2026-08-10): all 34 registered fields read; dpi_current=1, dpi_x/y gear0=800, gear1=5000, sensor_mode=0, config_name="CFG1", 0x08D8=0x00. Passive report 7 captured: gear/dpiX/dpiY MATCH the EEPROM read-back (5000==5000). Recorded ✅ in docs/FEATURES.md section 2.
- (answer) Q1 (formats): dpi_x_list/dpi_y_list confirmed 2-byte LE (raw 2003=800, indexed gear1 0x088A=0x1388=5000 == report7); dpi_current/dpi_enable_gear/sensor_mode and all B/C/E fields confirmed 1B; config_name 16B string. Buttons (D) stay open: 1B codes plausible (left=3) but a 2B read shows no consistent pattern (scroll bytes read 0xFF) — a write test (S8) is required to pin the format.
- (answer) Q3 (lift-off): MOUSE_SLIGHT 0x0884 reads 0x01 on the real device (product range 1.0-2.0 step 0.1); plausible 1.0 field — candidate confirmed as alive, exact scale pending UI write test (S7).
- (answer) Q6 (0x08D8 shared bit mask): CONFIRMED shared byte — rf_strengthen_switch and low_power_warn_switch both read 0x08D8=0x00 (bits 00000000). Model as a bit mask; per-field write must use masked values (deferred S6).
- (answer) Q7 (report 7 mirror): CONFIRMED for everything Phase 3 needs passively — gear, dpiX, dpiY, mode, rpt_24g, rpt_usb, config, battery all present in report 7; gear/dpiX/dpiY cross-validated MATCH vs EEPROM. rpt_24g/rpt_usb have no EEPROM field (adjustment ties to MOUSE_REPORT, S6).
- (event) S5 implemented + unit-tested (2026-08-11): performance.py (7-slot SENSOR_MODE table 0x08DC..0x08E2; set/read at 0x08DC+rate_index; rate maps 125/250/500/1000/2000/4000/8000 → codes 8/4/2/1/132/130/129; read_table/read_mode/set_mode/rate_index_from_code), window tab "Desempenho" with 6 radios, main.py wiring (_on_set_perf/_perf_changed/_refresh_perf/_current_perf_slot), battery.py exposes rpt_24g/rpt_usb from report 7, i18n pt/en/es. tests/test_performance.py: 12 tests; full suite 114 OK. On-device validation pending (mouse asleep): read 7 slots, confirm the active-rate slot, write+readback a mode and restore.
- (finding) S5 protocol: A Hub MousePerformanceUtil confirms mode value == mode id (0..5) and SENSOR_MODE is a per-rate table (write primitive x0 = one 0xA5 len-1 write at 0x08DC+offset). MOUSE_REPORT 0x0880 byte is the rateCode, not an index. product_info_rapoo.json 5197 ADDRESS.SENSOR_MODE = [0x08DC]×6 and MODES value==index corroborate.
- (decision) S5 UI: consistent with S4 — lives in the window tabs (not the tray submenu); the tray DPI submenu precedent applies.
- (event) S5 VALIDATED ON REAL DEVICE (2026-08-11): SENSOR_MODE slots read `[0,0,1,1,3,3,3]` (factory = low rates power-saving, high rates Hyper core); polling rate `0x0880`=0x01 rateCode → slot 3 (1000Hz); write+readback at slot 3 (mode 1→4→1) OK, restored; rate-code write (`0x0880`=8) mirrored in report 7 `rpt_usb` (=8; restore → =1) — CONFIRMS rpt_usb IS the rateCode (A Hub listener `rateCode === rpt_usb`) and rpt_24g is NOT (observed constant). Docs/FEATURES.md §B row + Polling rate row + CONTEXT.md B3 updated; settings.py registers all 7 SENSOR_MODE slots.
- (finding) S5 mode id→name is REVERSED vs A Hub UI order (bundle MousePerformanceUtil cards pair mode_0_title with id:5): device id **0 Office, 1 Balance (Low Power), 2 Fire (High Performance), 3 Hyper core, 4 Gaming hyper core, 5 Fury gaming (Corded)**. Fixed i18n pt_BR/en/es + performance.py docstring; GUI radio labels now correct.
- (finding) S5 selectable rules per rate slot (A Hub export `t`): {0:[0,1],1:[0,1],2:[0,1,2],3:[1,2,3,4,5],4:[2,3,4,5],5:[3,4,5],6:[3,4,5]}; implemented as perf.selectable_modes(slot) + gui set_sensitive.
- (answer) Q7 refined: report-7 mirror is NOT "same code" for both rpt bytes — `data[11]` (rpt_usb) = rateCode mirroring 0x0880; `data[10]` (rpt_24g) is a non-rate byte (observed 3 constant across 125/1000/2000Hz writes). main.py._current_perf_slot now prefers rpt_usb.
- (note) Observed device quirk during S5 validation: writing 0x0880=8 (125Hz) was followed by SENSOR_MODE slot 4 (0x08E0) drifting 3→2 on its own; restored to 3 (matches factory baseline) and verified it sticks. No write touched 0x08E0 — treat as firmware side effect when the rate changes.
