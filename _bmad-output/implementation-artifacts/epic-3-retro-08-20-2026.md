---
epic: 3
date: 08-20-2026
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 3 Retrospective — Phase 3: Performance / parameters (CAP-6)

## Epic summary

- **Epic**: 3 — Performance modes (3-1), RF + polling rate (3-2), §C mouse parameters/toggles/sliders (3-3). All `done`, `pending_stories: []`.
- **Diff range**: same NO_VCS situation — `baseline_commit: NO_VCS` on all three specs; implementation baked into root commit `95c2877` plus later evolution commits (8abae0c sliders/rate UI/RF radios, 4d083e review patches). Evidence narrowed to: `src/rapoo_vt7/performance.py`, `src/rapoo_vt7/parameters.py`, `tests/test_performance.py`, `tests/test_parameters.py`, the Desempenho tab region of `gui.py`, perf/params wiring in `main.py`.
- **Evidence inventory**:
  - Story specs with declared ACs: spec-3-1, spec-3-2, spec-3-3 ✅ — all three carry detailed Review Findings sections (applied patches + explicit defers) and change logs with on-device validation records
  - Sprint status ✅ · Previous retros: epic-1 and epic-2 (both ran today, items open) ✅
  - On-device validation records: CONTEXT.md B3 (modes/RF/rate 2026-08-11; §C write-test 2026-08-11; selectable sliders 2026-08-12; B3 closed 2026-08-19 with single follow-up P9) ✅
  - Session logs: absent → process-lesson analysis skipped

## Findings

Consolidated from one delegated analysis (deferred-item verification + aggregate views + three lenses) plus the behavior check. Sources are file:line in the current tree.

### Standing high gaps (wrong-but-green risk)

- **F1 (P9 — slider byte maps are inference)** — debounce/sleep/angle/lift-off maps in `parameters.py:50-56` are our inference ("not yet double-confirmed on-device"); the suite pins our own maps (`test_parameters.py:306-347`), so a wrong firmware mapping passes CI. Documented as B3's single follow-up (CONTEXT.md). Closure requires an A Hub write-diff. Disposition: **fix now** (Ask First on device).
- **F2 (0x08D8 bit layout hypothesis)** — bit0=RF/bit1=warn is a working hypothesis (`docs/FEATURES.md` §2.B ⚠️ row) pinned by tests (`test_performance.py:232-254,300-321`). Same wrong-but-green property; same A Hub write-diff closes it. Disposition: **fix now** (same session as F1).

### Medium

- **F3 (test-debt batch)** — 3-3's AC "a drag coalesces into one write" has zero tests (`gui.py:1262-1275`, no test touches `_on_param_scale*`); `_maybe_refresh_params` recovery untested while its perf sibling is (`test_performance.py:479-498`); language re-labeling of perf/rate/RF/§C widgets untested (`gui.py:1655-1673,1732-1739`) — the exact gap the applied 3-1 patch flagged; RF double-submit guard regression test never added (`gui.py:1288`); GUI-side `selectable_modes` sensitivity untested (`gui.py:1359-1366`). Disposition: **fix now**.
- **F4 (spec drift batch)** — spec-3-1's Spec Change Log is still empty despite the deferred instruction to record the split (`spec-3-1:79-81`); frozen "asleep → no mode change" matrix row contradicts shipped `wake=True` user actions (`main.py:326-331`); §C moved to its own "Parâmetros" tab rendered by `_render_perf` (`gui.py:475-485,1329-1434`) — no spec records the split; 3-2's rate-UI scope growth documented only post-hoc. Disposition: **fix now** (doc-only).
- **F5 (0x0884 double registration)** — `settings.mouse_slight` ("do not edit", `settings.py:89`) vs `parameters.lift_off` slider on the same offset (`parameters.py:39`); FEATURES.md §2.B "⚠️ don't edit" contradicts §2.C editable slider. Disposition: **fix now** (registry/doc reconciliation).
- **F6 (RMW lost-update vs external writer)** — `_write_shared_byte` read→modify→write→verify (`performance.py:116-120`) is serialized intra-app by the task queue but an external writer (A Hub) between read and write is silently lost; a write landing after the verify read passes verification. Inherent without an inter-process lock. Disposition: **accept** (document).

### Low / deferred (verified still open)

- `rpt_usb=0` → slot 0 via raw-index overlap instead of `SLOT_DEFAULT` (`performance.py:162-163`, pinned by `test_performance.py:100`) — needs a semantic decision.
- Signed-angle garbage decodes as a plausible in-span value (raw>127 wraps negative, `parameters.py:99-100`).
- External rate/mode/RF changes (A Hub) never detected — `_on_report` watches DPI only (`main.py:137-151`).
- Production `print()` debug noise (`main.py:144,189,798-801`).
- Param timers not cancelled on error renders (`gui.py:1265-1274` vs DPI's `_cancel_dpi_timers`).
- Duplication adds to epic-1 F8: `_read` ≡ copy in parameters.py; verify pattern ×4; grid-snap ×3 (`gui.py:1258-1261`, `gui.py:137-139`, `parameters.py:117-119`); FakeDev sets duplicated; RATE_HZ/PERF_SELECTABLE/i18n keys manual sync (×3 locales).
- Probe default flow still uncovered + bare `main()` returns 0 unconditionally (`probe.py:1047-1069`); `battery_probe` unguarded indexing remains (`probe.py:32-33`).
- Closed since the specs were written: tautological hypothesis test (replaced, `test_probe.py:227-251`); FEATURES §2.C header conflation (fixed, `FEATURES.md:63`); `write_baseline` fd leak (largely fixed, `probe.py:112-126`); status/dump main coverage added.

### Checked and clean

Masked-write bit preservation (tested incl. full-byte); readback-mismatch rejection on all four writers (mode/rate/toggle/slider, each tested); rpt_24g never used for slot selection (tested); per-field §C isolation with explicit timeout policy (tested); render-plan headless coverage for snap/disable/retention; locale parity + placeholder contract for every formatted key; baseline atomicity; `rate_mirror` cross-check in probe (tested); all ten applied review patches from 3-1/3-2 verified still in place (mode-radio retranslation, RF double-submit guard, out-of-range mode render guard, RF error isolation+retention, field whitelist, display-slot fix + selectable re-check, marked-but-disabled hint, placeholder test extension, rate_mirror check, FEATURES bullet fix).

## Behavior verification

- **Unit suite**: re-run during analysis → **Ran 510 tests, OK** (2026-08-20).
- **Live device run** (read-only): `performance.read_perf_state(dev, SLOT_DEFAULT)` → slot 3, mode 1 (Balance), RF raw 0; `parameters.read_section(dev)` → **11/11 §C bytes read, zero errors**. Read paths healthy end-to-end today.
- Write-path behaviors were validated on the real device at story time (modes/RF/rate 2026-08-11; §C write-test read→write→re-read→restore all 11 bytes 2026-08-11; sliders 2026-08-12 — CONTEXT.md B3). No write was performed during this retro.

## Previous-retro follow-through

Epics 1–2 retros ran earlier this session (2026-08-20); their action items are all `open`, none landed yet. Relevant overlap: epic-1 F5/F8 and epic-2 F5 sit on this epic's surface (`_read` helper duplication, multi-write atomicity, string codec).

## Action items

Confirmed by the user (2026-08-20). All `open` in sprint-status.yaml.

1. **P9 ampliado** — one A Hub write-diff session to pin BOTH the debounce/sleep/angle/lift-off byte maps AND the 0x08D8 bit layout (closes F1+F2; Ask First on device).
2. **Test-debt batch** — slider drag-coalescing test (declared AC, untested), `_maybe_refresh_params` recovery, language re-labeling of perf/rate/RF/§C widgets, RF double-submit regression, GUI-side `selectable_modes` sensitivity (F3).
3. **Spec-drift batch** — fill spec-3-1's empty change log; amend the frozen "asleep → no mode change" row vs shipped `wake=True`; record the Parâmetros tab split rendered by `_render_perf`; reconcile 3-2's state-only design note with the shipped rate UI (F4; doc-only).
4. **0x0884 conflict** — resolve the double registration (`settings.mouse_slight` "do not edit" vs `parameters.lift_off` slider) and the FEATURES.md §2.B vs §2.C contradiction (F5).
5. **RMW lost-update** — accepted: document that `_write_shared_byte` can lose an external writer's interleaved change (no inter-process lock); no code change planned (F6).
6. **rpt_usb=0 semantics** — decide slot 0 (current, pinned by test) vs `SLOT_DEFAULT` fallback for unknown/unavailable rate codes.

Deferred (not tracked as action items): signed-angle garbage decodes as plausible value; external A Hub changes undetected via report-7; production `print()` noise; param timers not cancelled on error renders; duplication additions feeding epic-1 F8; probe default-flow coverage + bare `main()` rc.

## Acceptance verdict

**accepted-with-open-items** (2026-08-20).

- All three stories' ACs substantively met; write paths validated on the real device at story time (CONTEXT.md B3, closed 2026-08-19); suite 510 OK and live read-only check healthy on retro day.
- Open items are honest: the two standing byte-map hypotheses (P9, 0x08D8) are documented as inference and pinned by tests that would go green even if the firmware disagreed — captured as action item 1 rather than silently accepted.
- Process note: this epic had the densest review record of the three (patches + defers logged per story), which made deferred-item verification cheap; spec-3-3 lacking a Findings section (patches only in its change log) was the one asymmetry.

## Open questions

- rpt_usb=0: is raw index 0 a legitimate "125 Hz active" or always garbage? Needs either a firmware answer or a deliberate decision (action item 6).
- Should external-change detection (A Hub writes while our app runs) ever be in scope, or permanently accepted? (feeds epic-1 F7 registry work if yes)
